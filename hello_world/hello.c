// include the standard input output library
#include <stdio.h>

// argc = number of arguments, argv = argument vector (array of the arguments)
// function returns 0 because everything is running OK

int main(int argc, char** argv) {
    puts("Hello, world!");
    return 0;
}